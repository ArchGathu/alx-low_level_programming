#include "main.h"

/**
 * _isdigit - check if number is a digit or character
 * @c: parameter
 *
 * Return: 1 if its a number and 0 otherwise
 */

int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
